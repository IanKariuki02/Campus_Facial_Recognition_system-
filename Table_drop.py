# import sqlite3
#
# # Connect to the SQLite database
# conn = sqlite3.connect('neoscapedb.sqlite')
# cursor = conn.cursor()
#
# # Drop the existing login_user table if it exists
# cursor.execute('''DROP TABLE IF EXISTS Photosample''')
#
# # Commit the drop operation
# conn.commit()
#
# # Close the connection
# conn.close()
